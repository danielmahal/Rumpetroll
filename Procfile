web: bundle exec em/daemon.rb -p $PORT >> /var/log/rumpetrolld.log 2>> /var/log/rumpetrolld-error.log

##For serving the website, but don't need it:
#web: bundle exec rackup -p $PORT
##Besides Dokku doesn't easily support two separate web processes
